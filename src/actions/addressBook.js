import { createAction } from 'redux-actions'

export const changeColor = createAction('changeColor')
export const updateTableList = createAction('updateTableList')
export const searchList = createAction('searchList')